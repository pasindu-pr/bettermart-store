export type PageLayoutProps = {
  children: React.ReactNode;
};

export type ProductCardTallProps = {
  id: string;
  name: string;
  price: string;
  smallDescription: string;
  imageSrc: string;
  imageAlt: string;
};
